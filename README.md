# YC-Builds-Paris

A static prototype for issuing real-world identities to AI agents.

Open `index.html` in a browser to use the local sandbox. It can issue a mock
agent identity with phone, email, card, and calendar credentials, simulate
webhook events, copy generated credentials, rotate keys, and export JSON.
