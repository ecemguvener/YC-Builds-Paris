2222

# Demo Video Pitch Script

Goal: make the judges feel the jump from "AI that replies" to "AI that follows through."

<!-- The message we are trying to give is: -->

AI agents already have brains. What they are missing is a real-world identity.

Our identity layer gives any agent a phone number, email address, payment capability, calendar, permissions, audit logs, and a kill switch. OpenClaw stays the brain. The identity layer is the passport and the hands.

The wow: the agent can start a task today, call people, email them, wait for replies, coordinate with other agents, and come back days later with a better answer than a one-shot chat response.

<!-- Script -->

### 0:00-0:35 - Hook

Ecem, to camera:

 Today, AI can think but What it is missing is a real-world identity. It cannot pick up the phone, call, pay for something, or wait days for a reply. 

They can write a plan, but they cannot execute the plan. That’s the biggest missing piece of agentic AI, AI has a brain but no identity.

ECEM 

So instead of building another agent, () we built missing layer; the infrastructure that gives every AI agent a real-world identity.

M

"This is not another chatbot. This is how agents start doing work in the real world. This is infrastructure for agentic workers. IT HAS IT'S OWN IDENTITIY , OWN PERSON. 

Screen: show the dashboard/chatbox or terminal ready to create an agent identity.

Here we can see that each agentic identity wth logs of phone etc ect 

### 0:35-1:30 - Create the Agent

Maxence:

"Let's hire our first AI employee, creating a new agent"

Screen: initialize identity. Show the agent name, runtime `openclaw`, and tools: phone, email, calendar, payment.

In under a minute, any agent including physical robot, openclaw, autonomous company builder gets their own identity which include phone number, payment access. We become everything that lets that brain operate in the real world.

Ecem:

Whats very amazing this isn’t tied to one framework.

You can connect an existing OpenClaw runtime…

or deploy it with any other agent.

Then we choose what this agent is allowed to use: phone, email, calendar, and payments etc Every permission is completely configurable.

Maxence:

"This matters because agents need power, but they also need boundaries. You do not want to hand an agent your raw credentials and hope nothing goes wrong."

Ecem:

This is the integration pain we remove. Normally you would have to connect voice providers like ElevenLabs, phone providers, payment cards, authorization, webhooks, and audit logs.
 ADD THIS TO WHY ARE WE BUILDING THIS

Ecem:

"Let's test the thing that makes it real to show the difference. Find a hairdresser near me, call them, ask whether they have availability this afternoon, and book the appointment.

If this were a normal chatbot, it would tell me:

“You should call the salon given their phone number.”

Instead…

I’m just going to ask the agent.

Prompt on screen:

```text
Find a hairdresser in Paris near Le Marais. Call them and ask if they have an available appointment this afternoon. For this demo, route the call to my phone.
```

Maxence:

Agent decides what to do next. Our identity layer executes them: send the email, make the call, create the calendar step, and record every action

Ecem:

The call is the most visual part, so for the demo we call our phone. But the real product is the whole automation layer."

"See, that's the moment. The agent did not just answer. It reached out."

Maxence:

"And now we can see the audit log. The call was allowed, who it was made by, and what identity token was used."

Screen: show call result and audit log entry: `phone.call allowed`.

Maxence:

"And if something goes wrong, we revoke the identity. The agent loses its real-world power immediately." //HOW

### 2:35-3:35 - Why This Is Infrastructure

Ecem:

"Why are we building this? Because we think the next wave is not just AI that talks. It is AI that operates."


"In the next months and years, more and more work will be automated. Agents will not just answer questions. They will call, pay, book and coordinate."


"But for that future to happen, agents need infrastructure. They need identity. They need permissions, payment and communication rails.

Maxence:

"We are building that now, before the trend becomes obvious, so we are ready when agentic work becomes real life."

"The reason we focused on infrastructure is that every serious agent builder hits the same wall. - The pain of finding a customer"

Ecem:

"Imagine the same thing, but not for one call. Imagine IN EVERY HACKATHON there are so many start ups but without no customer or no investor

Well with these agentic identities "One agent calls suppliers. Another books interviews. Another waits for replies until the job is actually done.

Screen: show a mock or real task queue with multiple agents: calling, emailing, waiting, following up, booking calls, summarizing results.

The wow factor is that an agent can take responsibility for a real task."

Maxence:

"Developers should not spend time to find customers to build before their agent can do one useful thing. They should initialize identity once, give the token to the agent, and let the agent act safely."

Ecem:

"This is the missing layer between AI reasoning and real-world execution."


Maxence:

"AI agents should not be trapped in a textbox."

Everyone is trying to build something very impressive but they are not thinking outside of the trapped AI box... Thats where we come to help you automise in real life with a real value. 








## Backup 60-Second Version

"AI can think, but it still cannot live in the real world. It cannot pick up the phone, send the email, pay for something, or wait three days for a reply.

We built the missing identity layer for agents. In under a minute, an OpenClaw agent gets a phone number, email address, payment access, calendar, permissions, audit logs, and a kill switch.

Here is the agent identity being created. OpenClaw only receives a scoped token, not our provider secrets. Now we ask it to call a hairdresser in Paris. The identity layer checks permission, places the call, and records the audit log. For the demo, the call routes to our phone. The point is simple: the agent did not just answer. It reached out.

Now imagine that running for a week. Agents calling suppliers, emailing customers, waiting for replies, booking interviews, and sharing results with each other. ChatGPT gives an answer from what it already knows. Our agents can go find out.

This is the difference between a conversation and a workforce. We give agents identity, permissions, and time, so they can finally get things done."

## Shot List

1. Camera hook with both founders.
2. Screen capture: create agent identity.
3. Screen capture: show phone, email, calendar, payment, permissions.
4. Screen capture: show scoped token and OpenClaw env vars.
5. Chatbox prompt: "call a hairdresser in Paris."
6. Phone rings; teammate answers as the hairdresser.
7. Screen capture: call result and audit log.
8. Screen capture: revoke/kill switch.
9. Vision visual: several agents running long-horizon tasks.
10. Final camera close.

## Lines to Emphasize

- "AI agents already have brains. What they are missing is a real-world identity."
- "The agent did not just answer. It reached out."
- "ChatGPT gives you an answer from what it already knows. Our agents can go find out."
- "This is the difference between a conversation and a workforce."
- "We give agents identity, permissions, and time."

## Do Not Say

- Do not use the old product name in the pitch.
- Do not say the hackathon demo already runs week-long research unless you show it as the vision or a designed capability.
- Do not overexplain Twilio, Gmail, or payment provider internals. Judges need to understand the value, not every integration detail.
- Do not pitch "customer finding" as the main product. The product is infrastructure: identity for agents. Customer discovery is just one killer demo.

## Architecture Proof Points for Judges

- `POST /api/identity/init` creates the agent identity and returns a scoped token.
- OpenClaw receives `IDENTITY_LAYER_API_URL` and `AGENT_IDENTITY_TOKEN`.
- Tool calls use bearer-token authorization.
- Phone, email, calendar, and payment actions are permission-gated.
- Every action writes to the audit log.
- The identity can be revoked, so the agent loses real-world power immediately.
- The OpenClaw skill tells the agent when to initialize identity and how to call tools safely.
